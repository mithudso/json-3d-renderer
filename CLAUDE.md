# CLAUDE.md — json-3d-renderer

Guidance for Claude Code (and other AI coding agents) working in this repository.

## Default Execution Strategy

- **Read before you write.** This repo is mostly *generated output* (HTML/JSON/Markdown
  visualizations) plus *vendored rendering libraries*. Confirm whether a file is
  hand-authored or machine-generated before editing — generated files carry a
  `GENERATED by …` banner on the first line and must not be hand-edited.
- **Never hand-edit generated artifacts.** Files with a `GENERATED by scripts/…` header
  are produced by build scripts that live in a *separate* upstream repo (see
  [docs/known-issues.md](docs/known-issues.md)). Editing them by hand will be lost on the
  next regeneration. Fix the upstream generator instead, or record the need as a TODO.
- **Do not invent commands, paths, env vars, or build steps.** There is no `package.json`,
  no build system, and no test suite checked into this repo today. If you need one, propose
  it explicitly — do not document a phantom workflow.
- **Keep large vendored files out of diffs.** `concept-tree-3d.bundle.js` (~1.6 MB) and
  `d3.v7.min.js` (~280 KB) are vendored builds. Treat them as opaque; do not reformat or
  re-minify them.

## What this repo is

A **JSON → 3D mind-map renderer** and a set of demo visualizations built with it. The
intent (per `README.md`) is to convert mind-map-style JSON into an interactive 3D map with
focus mode and node descriptions. Today the repo ships:

- The rendering libraries (`concept-tree-3d.bundle.js`, `d3.v7.min.js`) at the repo root.
- Self-contained demo pages under `examples/` (each inlines its data as `const DATA`).
- Generated Markdown / Mermaid companions under `examples/`.

See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) and
[docs/codebase-overview.md](docs/codebase-overview.md) for the full map.

## Repository layout (snapshot)

```
/
├── concept-tree-3d.bundle.js   # rendering library: three.js + 3d-force-graph (built bundle)
├── d3.v7.min.js                # rendering library: D3 v7 (2D radial / treemap / charts)
├── README.md · LICENSE
├── docs/                       # this documentation suite
└── examples/                   # demo pages, their inlined data, and generated companions
    ├── concept-tree-3d.html    # 3D mind map  (loads ../concept-tree-3d.bundle.js)
    ├── concept-tree.html       # 2D viz       (loads ../d3.v7.min.js)
    ├── skills-graph[-full].html
    ├── skills-graph[-full].json
    ├── tree.json               # sample source data (array of concept nodes)
    ├── concept-tree.md · tree.md · tree-mindmap.md   # generated Markdown / Mermaid
    └── README.md
```

> The `examples/*.html` pages load their library from the root via `../` (fixed 2026-06-20).
> Because they're generated files, the same fix must land upstream or it regresses — see
> [docs/known-issues.md](docs/known-issues.md).

## Workflow log rule

When you complete a unit of work in this repo, append a short entry to
[`memory.md`](memory.md) (what changed, why, next steps) and record the originating user
request in [`prompts.md`](prompts.md). Keep entries terse and in chronological order.

## Conventions

- ASCII by default; match the surrounding style of any file you touch.
- Markdown: ATX headings, fenced code blocks with language tags, reference real paths only.
- Data schemas are documented in [docs/COMPONENTS.md](docs/COMPONENTS.md) — keep them in
  sync if the JSON shape changes.

## Related agent instruction files

- [`AGENTS.md`](AGENTS.md) — repo-local agent catalog (currently none).
- [`GEMINI.md`](GEMINI.md) — Gemini CLI notes.
- `.github/copilot-instructions.md` — Copilot CLI rules (mirror of this file's strategy).
